// Example JavaScript to demonstrate minification
function greet(name) {
  const message = `Hello, ${name}!`;
  console.log(message);
}

// Call greet when loaded
document.addEventListener('DOMContentLoaded', function () {
  greet('world');
});
